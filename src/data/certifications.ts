import type { Certification } from "@/types/portfolio";

/**
 * No certifications held yet. The section renders nothing when this array is
 * empty (see `src/components/sections/Certifications.tsx`) — add entries here
 * once a credential is earned.
 */
export const certifications: Certification[] = [];
